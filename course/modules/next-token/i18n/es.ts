import type { ModuleUi } from "@/modules/types";

const ui: ModuleUi = {
  labTitle: "Inference Loop",
  labIntro: "24 bloques corrigen un vector; el último sale por la misma tabla; el token elegido vuelve a la entrada.",
};

export default ui;
