import type { ModuleUi } from "@/modules/types";

const ui: ModuleUi = {
  labTitle: "Inference Loop",
  labIntro: "24 blocks correct one vector; the last one leaves through the same table; the chosen token goes back to the input.",
};

export default ui;
