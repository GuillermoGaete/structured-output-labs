import type { ModuleUi } from "@/modules/types";

const ui: ModuleUi = {
  labTitle: "Inference Loop",
  labIntro: "One pass of the model per token: the chosen token goes back to the input and the network runs again.",
  tokensTitle: "Tokens",
  positions: "positions",
  template: "template",
  expandTemplate: "Show the chat-template tokens",
  lookup: "896 numbers · a table look-up, not arithmetic",
  blocksTitle: "24 blocks",
  block: "block",
  blocksCaption: "x ← x + attn(x) · x ← x + mlp(x) · every position looks only back",
  outputTitle: "Output layer",
  sameTable: "the same table",
  argmax: "argmax",
  sample: "sample",
  loopCaption: "the chosen token is appended and the pass runs again · one forward pass per token",
  hCaption: "last position, after the final norm",
  logitsPanel: "z = E[row] · h  (logits)",
  probsPanel: "p · the only thing the sampler sees",
  everythingElse: "everything else",
  tokens: "tokens",
  entropy: "entropy",
  choices: "choices",
  greedyNote: "Greedy is a mask: −∞ on everything but the max. Same trick as structured output, a different author of the list.",
  cutNote: "Struck-through rows were cut by top-k / top-p: their logit became −∞ and their probability 0.",
  temperature: "Temperature",
  greedy: "greedy (argmax)",
  topK: "top-k",
  topP: "top-p",
  off: "off",
  sampledTitle: "Chosen",
  pOfSampled: "p of the chosen",
  recordedPath: "recorded path",
  howTheDie: "how the die is rolled → Sampling Lab",
  noAttention: "This pass carried no attention weights.",
  layer: "layer",
  headMean: "mean of 14 heads",
  onlyBack: "only looks back",
  attentionAlt: "Attention arcs from the last position to the earlier ones",
  attentionCaption: "Each position looks at the positions before it and copies what is useful; the MLP makes each position think on its own. Arcs under 1 % hidden; the folded template sums its weights.",
  step: "step",
  stepButton: "Step",
  pause: "Pause",
  auto: "Auto",
  speed: "speed",
  speed_slow: "slow",
  speed_real: "real",
  speed_fast: "fast",
  reset: "Reset to prompt",
  editPrompt: "Edit prompt",
  running: "one pass running…",
  onePass: "one pass ·",
  stopped: "the model chose to stop",
  tabOutput: "Output",
  tabBlock: "Block",
  prevStep: "previous step",
  nextStep: "next step",
  preset_person: "Person (JSON)",
  preset_capital: "“The capital of France is”",
  preset_fibonacci: "“def fibonacci(n):”",
  preset_custom: "Your own prompt",
  promptLabel: "Prompt",
  chatTemplate: "Chat template",
  apply: "Apply and re-tokenize",
  seed: "Seed",
  maxSteps: "Max steps",
  rows: "Rows",
  recordedHint: "Recorded mode: presets replay the recorded run; the sampler only moves the bars.",
  liveHint: "Live: every Step is a real pass through the model.",
};

export default ui;
