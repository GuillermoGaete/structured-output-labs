import type { ModuleUi } from "@/modules/types";

const ui: ModuleUi = {
  labTitle: "Experiment Runner",
  labIntro: "A prompt makes `{` likely; it cannot make the fence impossible. Sometimes it fails.",
};

export default ui;
