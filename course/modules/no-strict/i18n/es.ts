import type { ModuleUi } from "@/modules/types";

const ui: ModuleUi = {
  labTitle: "Experiment Runner",
  labIntro: "Un prompt hace probable el `{`; no hace imposible el fence. A veces falla.",
};

export default ui;
