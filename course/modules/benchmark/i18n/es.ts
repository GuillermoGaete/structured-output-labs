import type { ModuleUi } from "@/modules/types";

const ui: ModuleUi = {
  labTitle: "Bench Lab",
  labIntro: "La restricción cuesta una compilación y un lookup por token; el forward domina; y ahorra reintentos.",
};

export default ui;
