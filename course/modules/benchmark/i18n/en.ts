import type { ModuleUi } from "@/modules/types";

const ui: ModuleUi = {
  labTitle: "Bench Lab",
  labIntro: "The constraint costs one compile and one lookup per token; the forward pass dominates; and it saves retries.",
};

export default ui;
