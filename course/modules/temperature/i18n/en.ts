import type { ModuleUi } from "@/modules/types";

const ui: ModuleUi = {
  labTitle: "Sampling Lab",
  labIntro: "The real logits of step 0. Move the temperature and watch the only thing the sampler sees.",
  promptLabel: "Prompt",
  preset_person: "Person (extract JSON)",
  preset_tree: "Tree (JSON tree)",
  preset_capital: "“The capital of France is”",
  preset_fibonacci: "“def fibonacci(n):”",
  preset_custom: "Your own prompt",
  chatTemplate: "Chat template",
  rows: "Rows",
  logScale: "Log scale",
  seed: "Seed",
  compare: "Two temperatures side by side",
  compareTitle: "The same distribution at two temperatures",
  recordedHint: "Recorded mode: only the example prompts have an answer.",
  liveHint: "Live: any prompt (one forward pass).",
  stepZeroIntro: "The model's scores for the first token: one for each of the",
  stepZeroIntroTail: "vocabulary entries. The bars on the left never move with the sliders: the model did not change.",
  temperature: "Temperature",
  greedy: "greedy (argmax)",
  topK: "top-k",
  topP: "top-p",
  off: "off",
  logits: "Logits (scores)",
  probabilities: "Probabilities (what the sampler sees)",
  everythingElse: "everything else",
  tokens: "tokens",
  entropy: "entropy",
  choices: "choices",
  greedyNote: "Greedy is a mask: −∞ on everything but the max. Same trick as structured output, a different author of the list.",
  cutNote: "Struck-through rows were cut by top-k / top-p: their logit became −∞ and their probability exactly 0.",
  die: "The loaded die",
  roll: "Roll",
  resetRolls: "Reset",
  landedOn: "Landed on",
  greedyDie: "greedy has no die: always the max",
  rollsSoFar: "rolls (observed vs expected)",
};

export default ui;
