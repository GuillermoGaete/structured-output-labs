import type { ModuleUi } from "@/modules/types";

const ui: ModuleUi = {
  labTitle: "Experiment Runner + Time Machine",
  labIntro: "exp(−∞) = 0: forbidden is not unlikely, it is impossible. It never fails.",
};

export default ui;
