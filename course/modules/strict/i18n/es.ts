import type { ModuleUi } from "@/modules/types";

const ui: ModuleUi = {
  labTitle: "Experiment Runner + Time Machine",
  labIntro: "exp(−∞) = 0: prohibido no es improbable, es imposible. Nunca falla.",
};

export default ui;
